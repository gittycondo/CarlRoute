// points.swift uses a graph data structure called "points" to connect to one another.
// These points are linked like a graph, but include information of their coordinates

Struct Point {
  var x: Double
  var y: Double
  var point: Point
}
